services:
  mcpo:
    build: .
    ports:
      - mcpo 
    volumes:
      - [rootPath where you put Installation Folder]:/rootPath
      - [path to output]:/output
    command: |
      mcpo --api-key top-secret --config /rootPath/config.json
networks: {}